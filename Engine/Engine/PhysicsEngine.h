#pragma once
#include "D3DUtils.h"
#include "Structures.h"
/*
class PhysicsEngine
{
public:
	PhysicsEngine();
	void Update(Object* object, const std::vector<Object*>& objects, const GameTimer& gt);
	void Reset(const std::vector<Object*>& objects);
	void ApplyVelocity(Object* object);
	float deltaTime = 0.0f;
	~PhysicsEngine();
private:
	const float gravity = -981.00f;
	void ApplyCollision(PhysicsObject* objectOne, PhysicsObject* objectTwo);
	void ApplyGravity(PhysicsObject* object);
	PhysicsObject* CheckCollision(PhysicsObject* object, const std::vector<Object*>& objects);
	void AdjustPosition(Object* object, float x, float y, float z);
	void CheckVelocity(PhysicsObject* object);
	bool CheckCollision(PhysicsObject* objectOne, PhysicsObject* objectTwo);
};*/